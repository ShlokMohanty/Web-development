.map() array method allows to run function on each item in the array , returning a new array as the 
result.
map() can be used to generate lists .
const myArray = ['apple', 'banana', 'Orange'];
const myList = myArray.map((item) => <p>{item}</p>)
